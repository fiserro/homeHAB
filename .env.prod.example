# homeHAB Production Environment Configuration
# Copy this file to .env.prod and customize for your production environment

# MQTT Broker
MQTT_BROKER_HOST=zigbee.home
MQTT_BROKER_PORT=1883
MQTT_CLIENT_ID=homehab-prod
MQTT_TOPIC_PREFIX=homehab

# Deployment (remote OpenHABian on Raspberry Pi)
DEPLOY_TYPE=remote
DEPLOY_TARGET=openhabian@raspberrypi.local:/etc/openhab

# Optional: SSH key path (if not using default ~/.ssh/id_rsa)
# SSH_KEY_PATH=~/.ssh/openhab_key
